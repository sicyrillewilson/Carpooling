<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!-- <style name="Base.Theme.CarPooling" parent="Theme.Material3.DayNight.NoActionBar"> -->
    <style name="Base.Theme.CarPooling" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:fontFamily">@font/itc_eras_demi_regular</item>
        <item name="android:textColor">@color/textColor</item>

        <item name="colorPrimary">@color/boutonColor1</item>
    </style>

    <style name="blackStyle">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">20sp</item>
        <item name="android:fontFamily">=@font/itc_eras_bold</item>
    </style>

    <style name="Theme.CarPooling" parent="Base.Theme.CarPooling" />
</resources>